html(ng-app='datapizz')

    head
        title Datapizz
        // fileblock:css css
        // endfileblock

    body(ng-controller="MainController as main")
        md-toolbar(layout='row')
            h1.md-toolbar-tools() Datapizz

            span.flex

            md-button(md-no-ink, ng-if='!main.connected', ng-click='main.connect()') Se connecter
            md-button(md-no-ink, ng-if='main.connected') Connecté

        section(layout='row', layout-align='center center', ng-controller='MainGraphController as graphCtrl' ng-if='main.connected')

            main-graph( collection='graphCtrl.entities', itemSelected='')

        // fileblock:js js
        // endfileblock
